<% content_for :header_title do %>Hantera orkestrar<% end %>


<div class="container">
  <a href="<%= url_for action: :index %>.csv" class="btn">
    Export all orchestra items
  </a>

  <a href="<%= url_for action: :extra_performances %>.csv" class="btn">
    Export extra performances
  </a>

  <a href="<%= url_for action: :anniversary %>.csv" class="btn">
    Export anniversaries
  </a>

  <a href="<%= url_for action: :allergies %>.csv" class="btn">
    Export allergies
  </a>

  <a href="<%= url_for action: :lintek_rebate %>.csv" class="btn">
    Export lintek
  </a>

  <table class="highlight">
    <thead>
    <tr>
      <th>Namn</th>
      <th>Ägare</th>
      <th>E-mail</th>
    </tr>
    </thead>
    <tbody>
    <% @orchestras.each do |orchestra| %>
        <tr data-href="<%= url_for controller: :orchestra, action: :show, id: orchestra['id'] %>">
          <td><%= orchestra['name'] %></td>
          <td><%= orchestra['user']['display_name'] %></td>
          <td><%= orchestra['user']['email'] %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>
