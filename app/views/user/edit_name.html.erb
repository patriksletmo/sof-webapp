<% content_for :header_title do %>Byt namn<% end %>

<div class="container">
  <div class="row section">
    <div class="col s12 m6">
      <div class="card grey lighten-4">
        <form method="post" id="edit-name-form">
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <div class="card-content">
            <span class="card-title">Byt namn</span>
            <div class="input-field">
              <input type="text" name="display_name" id="display_name" required>
              <label for="display_name">Nytt namn</label>
            </div>
          </div>
            <div class="card-action">
              <a class="blue-text" href="javascript:;" onclick="document.getElementById('edit-name-submit').click();">Skicka</a>
            </div>

            <input id="edit-name-submit"
                   type="submit"
                   style="position: absolute; left: -9999px; width: 1px; height: 1px;"
                   tabindex="-1" />
        </form>
      </div>
    </div>
  </div>
</div>